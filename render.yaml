services:
  - type: worker
    name: crawler
    runtime: docker
    plan: starter
    numInstances: 3
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: crawler-db
          property: connectionString

databases:
  - name: crawler-db
    plan: starter
